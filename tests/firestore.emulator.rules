// EMULATOR TESTS ONLY. DO NOT deploy to the shared real Firebase project.
// Existing AI Studio app's rules must be inspected and preserved independently.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
